name: docker build image master
on:
  push:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Source
      uses: actions/checkout@v1
    - name: Docker Login
      run: docker login -u awesomepayne -p wzp071912
    - name: Build AND push the Docker Image
      run: |
        tag=${GITHUB_REF:11}
        echo "Build Tag '$tag'"
        docker build -t awesomedjango/gerapy:$tag -f ./docker/Dockerfile .
        docker push awesomedjango/gerapy:$tag
        regex='^([0-9]+\.){0,2}(\*|[0-9]+)$'
        if [[ $tag =~ $regex ]]; then
          echo "Build Stable Version '$tag'"
          docker tag awesomedjango/gerapy:$tag awesomedjango/gerapy:latest
          docker push awesomedjango/gerapy:latest
        fi